# phonetics.md — 発音チェック機能の仕様

**大前提：サーバーを立てない。有料 API を使わない。可能な限りブラウザ内で完結させる。**

---

## 0. なぜ「音声認識」だけでは足りないか

「音声入力して合ってるか返す」を素朴に実装すると、音声認識 API に投げて
文字列一致を見るだけになる。これは**次の理由で発音練習には不十分**：

- 認識エンジンは言語モデルを持っているので、**下手な発音を正しい単語に「補正」してしまう**（偽陽性）
- 逆に、孤立した単語は文脈がないため認識に失敗しやすい（偽陰性）
- 「合ってる／間違ってる」の 2 値しか返らず、**何をどう直せばいいかが分からない**

そこで **3 層構成**にする。層が増えるほど「何を直すか」が具体的になる。

| 層 | 手法 | 出力 | オフライン | 対応ブラウザ |
|---|---|---|---|---|
| **L1** | Web Speech API による認識マッチ | 合／否 | ✗ | Chrome / Edge のみ |
| **L2** | MFCC + DTW で参照音声と波形比較 | 0–100 の類似度 | ✓ | 全ブラウザ |
| **L3** | 音響特徴の直接測定（VOT・フォルマント） | 具体的な数値と指示 | ✓ | 全ブラウザ |

**優先度は L3a（VOT）> L1 > L2 > L3b/c。** 理由は §3 を参照。

---

## 1. L1 — 認識マッチ（Web Speech API）

```ts
const rec = new (window.SpeechRecognition ?? window.webkitSpeechRecognition)();
rec.lang = 'hy-AM';
rec.interimResults = false;
rec.maxAlternatives = 5;   // 上位候補も見る
```

- **無料・APIキー不要。** Chrome では Google の音声認識エンジンが使われる
- `hy-AM` の対応状況はブラウザとバージョンに依存する。**必ず起動時に実地で確認し、
  ダメなら L1 を無効化して L2/L3 だけで動かす**（機能検出をハードコードしない）
- 判定は上位 5 候補と目標語を正規化して比較（句読点除去、`ը` の揺れ吸収、大文字小文字統一）
- 孤立語は認識されにくいので、**キャリア文に埋め込んで読ませる**
  例：`Ասա՛ «___»։`（「___」と言って）

### ⚠️ プライバシー
L1 は**音声を Google のサーバーに送信する**。これは明示的にユーザーに開示し、
**オプトイン（既定オフ）**にする。L2/L3 は端末内で完結するので既定オンでよい。

---

## 2. L2 — 波形類似度（MFCC + DTW）

完全にブラウザ内で完結。参照音声（ネイティブ録音）との距離を測る。

### パイプライン
```
getUserMedia (16kHz mono)
  → プリエンファシス (0.97)
  → フレーム分割 (25ms 窓 / 10ms シフト)
  → MFCC 13次元         [Meyda を使用]
  → CMVN（ケプストラム平均分散正規化）※話者差の吸収に必須
  → Δ・ΔΔ を連結 (39次元)
  → DTW で参照系列と対応付け
  → 正規化累積距離 → 0–100 にマッピング
```

### 既知の弱点と対策
- **話者差に弱い**（声の高さ・音色・話速が違うと距離が開く）
  → CMVN を必ず入れる。加えて**絶対スコアではなく「自分の過去最高との比較」を主に見せる**
- 無音・雑音区間が距離を汚す → VAD（簡易でよい：短時間エネルギー + ゼロ交差率）で前後をトリム
- **スコアの見せ方に注意。**「87点」は精密に見えるが実態は曖昧。
  5 段階（もう一息／通じる／良い／かなり良い／ネイティブ級）程度の粒度に丸める

### 参照音声の調達（無料・ライセンス明確なもののみ）
- **Mozilla Common Voice（hy-AM）** — CC0。文単位の録音。最有力
- **Wikimedia Commons の発音ファイル** — CC BY-SA。単語単位。要クレジット表示
- **自前録音** — アルメニア語話者に協力を依頼できるならこれが最良

> ライセンス不明の音源を `content/` に入れないこと。出典とライセンスを
> `audio-credits.json` に必ず記録する。

---

## 3. L3 — 音響特徴の直接測定

**ここが本命。** 日本語話者がアルメニア語で間違える箇所は分かっているので、
そこを狙い撃ちで測定すれば、汎用スコアより遥かに役立つフィードバックが返せる。

### 3a. VOT 測定 — 三系列の破裂音（最優先機能）

**なぜ最優先か：**
アルメニア語は破裂音に **有声／無気無声／帯気無声** の 3 項対立がある（`բ / պ / փ`）。
日本語は 2 項（有声／無声）しかなく、しかも日本語の語頭無声破裂音の VOT は
**ちょうど `պ` と `փ` の中間あたり**に落ちる。つまり日本語話者は放っておくと
「どちらでもない音」を出し続け、しかも自分では気づけない。

VOT（Voice Onset Time＝破裂から声帯振動開始までの時間）は**波形から数値で測れる**ので、
「あなたの `պ` は 58ms。それは `փ` の領域です」という具体的な指摘ができる。

**測定アルゴリズム**
1. **バーストの検出**：高域（>2kHz）短時間エネルギーの急峻な立ち上がり点。
   広帯域エネルギーの時間微分の最大値でも近似できる
2. **有声開始の検出**：自己相関による周期性（F0）の検出開始点、
   または低域（<500Hz）エネルギー上昇 + ゼロ交差率の急落
3. `VOT = 有声開始時刻 − バースト時刻`
4. **前有声化（prevoicing）**：バースト**より前**の低域（<300Hz）に周期的エネルギーがあれば
   VOT は負。`բ` の判定はこれ

**目標レンジ（※実装前に必ず較正すること）**

| 文字 | 種別 | VOT の目安 |
|---|---|---|
| `բ` /b/ | 有声 | 負（前有声化あり） |
| `պ` /p/ | 無気無声 | 短い正の値 |
| `փ` /pʰ/ | 帯気無声 | 長い正の値 |

> ⚠️ **具体的なミリ秒の閾値をこの表に書き込む前に、必ず較正すること。**
> 東アルメニア語の VOT には音声学の実測研究があるので文献値を参照し、
> あわせて §2 の参照音声から実測して閾値を決める。
> 推測で閾値を決めると、学習者に間違った発音を教え込むことになる。

**対象とする 5 組**：`բ/պ/փ`・`գ/կ/ք`・`դ/տ/թ`・`ձ/ծ/ց`・`ջ/ճ/չ`
（後ろ 2 組は破擦音なので、バースト検出をフリケーション区間に読み替える必要がある。
まず破裂音 3 組だけで作り、破擦音は後回しでよい）

**UI**：1 次元の数直線上に 3 つの目標ゾーンを描き、自分の発音を点でプロットする。
録音を重ねると点が目標ゾーンに寄っていくのが見える。**この画面が本アプリの目玉。**

### 3b. 母音フォルマント（F1/F2）

アルメニア語の母音は 6 つ（`ա /ɑ/`, `ի /i/`, `ու /u/`, `ե,է /ɛ/`, `ո,օ /o/`, `ը /ə/`）。
数が少なく体系も素直なので、F1/F2 プロットが有効。

- LPC 分析（次数 ~12）または FFT のピーク追跡で F1・F2 を推定
- 母音四辺形上に自分の点と目標楕円を重ねて表示
- **日本語話者の要注意点**：`ը /ə/` は日本語に対応音がない（曖昧母音）。
  日本語の「ウ」で代用しがち。`ի` と `ե` の区別も緩みやすい
- 声道長の個人差があるため、目標楕円は話者の F3 か F0 で正規化するか、
  最低限「男声／女声」で切り替える

### 3c. 日本語話者に無い子音

| 文字 | 音 | 日本語話者の典型的な誤り | 測定の手がかり |
|---|---|---|---|
| `ղ` | /ʁ/ 有声口蓋垂摩擦音 | 「ガ」や「ラ」で代用 | 低域の有声摩擦ノイズ、スペクトル重心が低い |
| `խ` | /χ/ 無声口蓋垂摩擦音 | 「ハ」で代用 | 摩擦ノイズのスペクトル重心（/h/ より低い） |
| `ռ` vs `ր` | ふるえ音 vs はじき音 | 両方とも日本語の「ラ」になる | **振幅の周期的変調**。ふるえ音は数回の閉鎖パルスが並ぶ。パルス数を数えれば区別できる |
| `հ` | /h/ | ほぼ問題なし | — |

`ռ`/`ր` の区別はパルス計数で比較的素直に実装できる。**3a の次に着手する候補。**

---

## 4. 実装上の共通ルール

- **録音は 16kHz mono、`AudioWorklet` で取得**（`ScriptProcessorNode` は非推奨）
- DSP は `src/domain/phonetics/` に**純粋関数として置き、必ずテストを書く**。
  テストには合成信号（既知の VOT を持つ人工波形）を使う
- マイク権限が拒否された場合、**発音機能だけが無効化され、他は動く**こと
- 静かでない環境でも壊れないこと。SNR が低いときは判定せず
  「もう一度、静かな場所で」と返す。**推測でスコアを出さない**
- 録音データは端末内に留め、既定では保存しない（保存はユーザーが明示的に選択）

---

## 5. フィードバックの書き方

「発音が違います」は情報量ゼロ。**何を、どちらの方向に、どれだけ**動かすかを言う。

| 悪い例 | 良い例 |
|---|---|
| 不正解です | `պ` の VOT が 58ms でした。`փ` に寄っています。息を弱く、破裂の直後すぐ声を出してください |
| 78点 | 「通じる」レベル。前回より `ղ` が改善しています |
| もう一度 | 声が小さすぎて測定できませんでした。マイクに近づいてください |

エラーは謝らない。何が起きたかと、次にどうするかだけを書く。
