---
paths:
  - "src/features/**"
  - "src/components/**"
  - "src/**/*.css"
  - "tailwind.config.ts"
---

# UI・インタラクションのルール

参照元は「アルメニア写本」。マテナダラン（Մատենադարան）所蔵の中世彩飾写本
——朱（վերմիլիոն）、ラピスラズリの青、金箔、羊皮紙——を出発点とする。
文字そのもの（405年にメスロプ・マシュトツが設計した人工文字）が主役。

- 大文字書体 **երկաթագիր（エルカタギル／「鉄の文字」）** の幾何学的な骨格を
  見出しの造形に反映させる。装飾フォントとして使うのではなく、字形学習の教材として見せる。
- 派手さは「文字を見せる場所」1箇所に集中させ、それ以外は静かに保つ。
- 汎用的な AI っぽい配色（クリーム地＋セリフ＋テラコッタ、黒地＋蛍光アクセント）は避ける。
- 品質の下限：モバイル対応、キーボードフォーカスの可視化、`prefers-reduced-motion` の尊重。

UI 文言は日本語。能動態、体言止めを避け、ボタンの語をフロー全体で統一する。

### 出題形式の原則
**4択を既定の出題形式にしない。** 認識 < 想起 < 産出の順に効果が上がるので、
「選ばせる」より「書かせる・打たせる・言わせる」を優先する。
4択が許されるのは知覚訓練（音や字形の聞き分け・見分け）だけ。詳細は `docs/interaction.md`。
