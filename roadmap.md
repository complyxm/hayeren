# roadmap.md — 実装ロードマップ

**最終目標：エレバンで生活できること。** 各フェーズはその一歩として設計されている。

**フェーズは順番に進める。先のフェーズに手を出さない。**
開始時に実装計画を提示し、完了時に完了条件との対応を報告すること。

---

## Phase 0 — 基盤とデプロイ（0.5 日）

**「最後にデプロイ」は禁止。この時点で本番 URL を通す。**

- Vite + React + TS + Tailwind + Vitest + `vite-plugin-pwa`
- `Noto Sans Armenian` / `Noto Serif Armenian` を self-host（CDN 依存にしない）
- Dexie スキーマ初版（`cards`, `reviews`, `settings`）
- Zod スキーマと `scripts/validate-content.ts`
- GitHub Actions（lint + test + validate）
- **Cloudflare Pages に接続し、push で自動デプロイされる状態にする**

**完了条件**
- [ ] 本番 URL でトップ画面が開く
- [ ] `Բարև ձեզ։` が正しいフォントで描画される
- [ ] `npm run lint && npm run test && npm run validate` が通る
- [ ] 機内モードでリロードしても起動する
- [ ] ホーム画面に追加でき、全画面で起動する
- [ ] **課金要素がゼロであることを確認して報告する**

---

## Phase 1 — 文字モジュール（2–3 日）

**このフェーズ単体で価値がある状態にする。**

- `content/alphabet.json`（38 字母 + `ու` + `և`）を Zod 検証つきで作成
- 文字一覧（大文字／小文字／字名／転写／IPA）
- 文字詳細：筆順アニメーション（SVG）、その文字で始まる単語 3 語
- **なぞり書き**（識別4択の置き換え。指で書き、筆順が違えば止まる）
- **聞き分けチャレンジ**（似た音の2択 + 反応時間記録）
- 画面内アルメニア語キーボード（音声配列）
- 句読点の解説カード（特に `՞` の位置）
- **エレバン地下鉄ナビ**（駅名を読んで経路を答える。最初の「読めた」体験）

> 出題形式は `docs/interaction.md` に従う。**4択に逃げないこと。**

**完了条件**
- [ ] 全 38 字母 + 2 が表示され、Unicode 範囲外の文字の混入がないことをテストが保証する
- [ ] 筆順アニメーションが全文字で動く
- [ ] なぞり書きで全文字を書け、筆順違いが検出される
- [ ] 聞き分けチャレンジで正誤と反応時間が記録される
- [ ] 画面内キーボードで `Հայերեն` と入力できる
- [ ] 地下鉄の全駅名を読む課をクリアできる
- [ ] `prefers-reduced-motion` でアニメーションが止まる

---

## Phase 2 — SRS 基盤（1–2 日）

- `ts-fsrs` を `src/domain/srs/` にラップ（アプリの都合を FSRS に漏らさない）
- Dexie にレビュー履歴を永続化
- レビュー画面（表 → 裏 → 4 段階評価）
- 1 日の新規カード上限設定
- 進捗の JSON エクスポート／インポート

**完了条件**
- [ ] Phase 1 の文字がカード化され、日をまたいで復習キューに正しく並ぶ
- [ ] SRS ロジックのユニットテスト（境界ケース含む）が通る
- [ ] エクスポート → 全削除 → インポートで進捗が完全復元される

---

## Phase 3 — 発音チェック（コア）（3–5 日）

仕様は `docs/phonetics.md`。**ここが本アプリの差別化点。**

### 3-1. 録音基盤
- `AudioWorklet` で 16kHz mono 録音、簡易 VAD で前後トリム
- マイク拒否時に他機能が壊れないことを確認

### 3-2. VOT 測定（最優先）
- バースト検出 / 有声開始検出 / 前有声化検出を純粋関数で実装
- **合成波形（既知の VOT を持つ人工信号）でテストを書く**
- 参照音声から実測して閾値を較正する。**推測値をハードコードしない**
- 三系列プロット UI（数直線 + 目標ゾーン + 自分の点）
- 対象は破裂音 3 組（`բ/պ/փ`, `գ/կ/ք`, `դ/տ/թ`）。破擦音は後回し

### 3-3. 認識マッチ（L1）
- Web Speech API `hy-AM`。**実地で対応状況を確認**し、ダメなら無効化して先へ進む
- 既定オフのオプトイン。外部送信であることを画面に明記

**完了条件**
- [ ] 合成信号に対する VOT 測定の誤差が許容範囲内であることをテストが示す
- [ ] 閾値の根拠（文献値／実測値）がコードのコメントに記録されている
- [ ] `պ` と `փ` を意図的に撃ち分けたとき、プロット上で点が別ゾーンに落ちる
- [ ] マイク権限を拒否してもアプリ全体が動く
- [ ] 雑音下では判定せず「静かな場所で」と返す
- [ ] 音声が外部送信される機能が既定オフになっている

> ⚠️ このフェーズは**うまくいかない可能性がある**。
> 検出精度が実用に届かない場合、無理に押し通さず、
> 「参照音声を聞いて自分の録音と聴き比べる」だけの簡易版に落とす判断をユーザーに相談すること。

---

## Phase 4 — 語彙モジュール（3–5 日）

- `content/vocab/` にエレバン生活順のテーマ別 JSON（第 1 段階 500 語）
- **`status: "verified"` の語だけを出題。** 未検証語はレビュー待ち画面に隔離
- 単語詳細（用例文、不規則形、音声）
- `hy → ja`（認識）/ `ja → hy`（**タイピング想起**）の 2 方向カード。
  `ja → hy` は4択ではなく画面内キーボードで打たせる
- **`ru_cognate` フィールドをスキーマに追加**（中身は空でよい。後から足すと高くつく）
- 音声スプライトのビルドスクリプト（ファイル数上限対策）
- `audio-credits.json` にライセンスを記録

**完了条件**
- [ ] 500 語すべてに用例文と `source` がある
- [ ] 未検証語が出題キューに混入しないことをテストが保証する
- [ ] `audio` が無い語でも UI が壊れない
- [ ] 音声がスプライト化され、デプロイのファイル数上限に余裕がある

> ⚠️ このフェーズが最も「それらしい嘘」を生みやすい。
> 一度に大量生成せず、**50 語ずつ作ってユーザーに検証を依頼する**こと。

---

## Phase 5 — 文法モジュール（4–6 日）

- 活用・格変化エンジン（`src/domain/grammar/`）を**テストファースト**で実装
- `exceptions.json`（不規則動詞・不規則複数形）
- `content/grammar/L01`–`L24`
- 課の画面：解説 → 例文 → 練習
- **文タイル**（肯定↔否定で助動詞 `եմ` が飛ぶ変形を体で覚える）
- **活用マシン**（ダイヤルを回すと形が即座に変わる。正誤判定なしの探索画面）
- 練習結果を SRS カード化

**完了条件**
- [ ] `conjugate()` / `decline()` が全 24 課の例文に対して正しい形を返す
- [ ] 不規則語で例外辞書が規則より優先されることをテストが保証する
- [ ] L01–L24 が前提課の順に解放される

---

## Phase 6 — エレバンモード（2–3 日）

`docs/curriculum.md` §7 を実装する。

- `content/scenarios/` の場面ユニット（パン屋 / マルシュルートカ / 薬局 / 大家 / 銀行…）
- **分岐ダイアログ**（手書きツリー。AIは使わない。失敗を笑いとして設計する）
- 到達度メーター（語数ではなく「通過できる場面」で表示）
- 看板・メニューの読解課（ライセンスの明確な画像のみ）
- 目標日カウントダウン（任意設定）
- 通知ベースのリマインダー

**完了条件**
- [ ] 場面の通過判定が SRS の状態と正しく連動する
- [ ] 到達度メーターが「あと何語で通過」を具体的に示す
- [ ] 使用画像すべてに出典とライセンスの記録がある
- [ ] 目標日を設定しなくても全機能が動く

---

## Phase 7 — ロシア語レイヤー（3–4 日）

仕様は `docs/russian.md`。**Phase 6 の場面ユニットが完成してから着手する。**

- `content/ru/` に場面別フレーズ集（200〜300 フレーズ。これ以上増やさない）
- **解放条件**：その場面のアルメニア語カードが SRS で安定していること
- レビューキューをアルメニア語と完全に分離
- 言語切り替えの UI（**併記しない**。タブまたはトグル）
- `ru_cognate` の中身を埋め、「ロシア語由来」バッジを表示
- 文法 L02 に「アルメニア語に性はない」の警告を追加

**完了条件**
- [ ] アルメニア語が安定していない場面のロシア語が解放されないことをテストが保証する
- [ ] 同一セッションで両言語のカードが混在しないことをテストが保証する
- [ ] 2言語が同じ画面に併記される箇所が存在しない
- [ ] キリル文字の学習コンテンツを作っていない（不要なので）
- [ ] 丁寧さのレベル（ты / вы）が検証済みで、`unverified` が出題されない

> ⚠️ ここでロシア語を膨らませ始めると、アルメニア語が止まる。
> 200〜300 フレーズを超えそうになったら**実装を止めてユーザーに相談する。**

---

## Phase 8 — 発音チェック（拡張）（2–4 日）

Phase 3 が実用水準に達している場合のみ着手する。

- L2：MFCC + DTW による参照音声との類似度
- L3b：母音フォルマント（F1/F2）プロット
- L3c：`ղ`/`խ` の摩擦音判定、`ռ`/`ր` のパルス計数
- 破擦音（`ձ/ծ/ց`, `ջ/ճ/չ`）への VOT 拡張

---

## Phase 9 — 仕上げ（2–3 日）

- ダッシュボード（今日の復習、到達度、発音の推移）
- 設定（転写の表示切替、新規カード上限、フォント、マイク関連）
- デザインの磨き込み（`CLAUDE.md` §10）
- アクセシビリティ監査

**完了条件**
- [ ] 初回起動から学習開始まで 3 タップ以内
- [ ] キーボードのみで全機能が操作できる
- [ ] Lighthouse の PWA / アクセシビリティが 90 以上
- [ ] **運用コストが月 0 円であることを構成ごと確認して報告する**

---

## 将来の拡張（今は作らない）

- 西アルメニア語対応（`dialect` フラグの切り替えで済む設計になっているか Phase 0 で確認）
- 手書き認識
- 会話シャドーイング
- 文化コンテンツ（音楽・映画・料理）の拡充
