# CLAUDE.md — Hayeren（アルメニア語学習アプリ）

このファイルは Claude Code がこのリポジトリで作業する際の常時参照メモリです。
詳細仕様は `docs/` を参照してください。

---

## 0. 確定事項（勝手に変更しない）

| 項目 | 決定 | 備考 |
|---|---|---|
| **方言** | **東アルメニア語（Eastern / Հայաստանի）** ✅確定 | 最終目標がエレバン生活のため。西アルメニア語（ディアスポラ）とは子音の発音がほぼ全面的に入れ替わる |
| **正書法** | **改革正書法（Reformed）** ✅確定 | 東アルメニア語の標準。古典正書法は使わない |
| 学習者の母語 | 日本語 | 訳語・解説は日本語。ローマ字転写は補助扱い |
| 最初のマイルストーン | CEFR A2 相当 | 文字 → 発音 → 基本文法 → 生活語彙 2,000 語 |
| プラットフォーム | Web（PWA / オフライン動作） | ネイティブアプリは作らない |
| コスト | 恒久的に 0 円（§2） | 例外なし |
| ロシア語 | **Phase 7 で実用レイヤーを追加** | エレバンでの実用目的。学習者はキリル文字を読めるので文字学習は作らない。詳細は `docs/russian.md` |

**方言フラグは全コンテンツデータに `dialect: "east" | "west"` として持たせること。**
現時点で値は `"east"` のみだが、将来スキーマを壊さないために必ずフィールドを置く。

**発音・文法・語彙のすべてを東アルメニア語で統一する。**
西アルメニア語の資料を参照した場合は、そのまま取り込まず必ず東に変換し、
変換したことを `source` に記録すること。ネット上のアルメニア語教材は
西アルメニア語のものが混ざっているため、ここは特に注意する。

---

## 1. プロダクトの定義

**最終目標：ユーザーがエレバン（Երևան）で生活できるようになること。**

そこから逆算して、日本語話者がアルメニア語の **文字・発音・基本文法・生活語彙** を
ゼロから独習するための、オフラインで動く間隔反復（SRS）ベースの Web アプリ。

この最終目標は飾りではなく、判断基準として使う。機能や語彙の優先順位に迷ったら
**「エレバンで暮らす人が、より早く困らなくなるのはどちらか」**で決める。

**非目標（作らないもの）**：会話AI、コミュニティ機能、課金、アカウント同期、
サーバーサイド全般、ポイント／バッジ等の装飾的ゲーミフィケーション。

---

## 2. コスト制約（絶対条件）

**開発中は金銭的コストを一切発生させない。** 「なるべく安く」ではなく設計上の制約として扱う：

- **サーバーを持たない。** バックエンド、データベース、認証、いずれも作らない
- **有料 API を使わない。** APIキーが必要な機能は原則却下。
  どうしても必要なら「ユーザーが自分の鍵を入れる任意機能」にし、無くても全体が動くこと
- **無料枠に依存する機能を必須経路に置かない。** 無料枠が消えたら壊れる設計にしない
- 新しい依存やサービスを提案するとき、**Claude Code は必ず「これは無料か、いつまで無料か」を明記する**

**例外はネイティブ配布の費用のみ**（Apple Developer Program 等。Phase 9 以降に個別判断）。
**開発中の 0 円は例外なく維持する。この例外を根拠に有料 API やサーバーを提案しないこと。**

### デプロイ先：Cloudflare Pages（静的ホスティング）

- 完全な静的サイトとしてビルドする。SSR・Edge Functions を使わない
- **Phase 0 の時点でデプロイまで通す。**「最後にデプロイ」は禁止
- 気をつける無料枠の上限：
  - **1 デプロイあたりのファイル数**に上限がある → 単語ごとに mp3 を 1 ファイル置くと
    早晩ぶつかる。**音声はスプライト（連結音声＋タイミング JSON）にまとめる**前提で設計する
  - 1 ファイルあたりのサイズ上限もある → 巨大な音声・モデルファイルを置かない
  - 上限の具体値は変わるので、**実装時に Cloudflare の公式ドキュメントで確認すること**
- 音声が肥大化したら Cloudflare R2（エグレス無料）への退避を検討する。判断はユーザーに確認

## 3. 配布形態：PWA

- **ネイティブアプリは作らない。** Vite の PWA ビルドでホーム画面に追加できる形にする
- 得られるもの：ホーム画面アイコン、全画面起動、オフライン動作、プッシュ通知
- 復習のリマインダーは通知で実装する
  （iOS ではホーム画面に追加した PWA でのみプッシュが動く点に注意）

## 4. 技術スタック

- **Vite + React 18 + TypeScript**（strict モード）
- **Tailwind CSS**（デザイントークンは `tailwind.config.ts` に集約）
- **Dexie**（IndexedDB ラッパー）— 学習進捗の永続化
- **ts-fsrs** — 間隔反復アルゴリズム（FSRS）。SM-2 を自前実装しない
- **Web Audio API + AudioWorklet** — 録音と音響解析（`docs/phonetics.md`）
- **Meyda** — MFCC 等の特徴量抽出。VOT 測定は自前 DSP で書く
- **Zod** — コンテンツ JSON のスキーマ検証
- **Vitest + Testing Library** — テスト
- **vite-plugin-pwa** — オフライン対応

すべてクライアントサイド。**ビルド成果物は静的ファイルのみ。**

### 依存を追加する前に
新しいライブラリを入れる前に必ず理由を1行で述べ、既存スタックで代替できないか、
**そして無料かどうか**を確認すること。

---

## 5. ディレクトリ構成

```
CLAUDE.md              # 常時読み込み（200行以内に保つ）
.claude/rules/         # 対象ファイルに触れたときだけ読み込まれるルール
docs/                  # 必要になった時点で読む詳細仕様
  curriculum.md        #   文字/文法/語彙/エレバンモード
  phonetics.md         #   発音チェックの技術仕様
  interaction.md       #   インタラクション設計（4択に逃げない）
  russian.md           #   ロシア語レイヤー（Phase 7）
  roadmap.md           #   フェーズ別タスクと完了条件
content/               # ★コンテンツデータ（コードから完全分離）
  alphabet.json / grammar/ / vocab/ / scenarios/ / ru/ / audio-credits.json
src/
  domain/              # 純粋ロジック・副作用なし・テスト必須
    srs/ grammar/ phonetics/ translit/
  data/                # Dexie スキーマ、コンテンツローダー
  features/            # 画面単位 alphabet / vocab / grammar / review / speak
  components/          # 汎用UI
scripts/               # validate-content.ts / build-audio-sprite.ts
```

**鉄則：`content/` の下にコードを書かない。`src/` の下に語彙や例文をハードコードしない。**

---

## 6. アルメニア語を扱う上での必須ルール

これを守らないとアプリが根本的に壊れます。

1. **Unicode 範囲は U+0530–U+058F。** 見た目の似たラテン文字・キリル文字を混入させない
   （例：`o` ではなく `օ`、`h` ではなく `հ`）。テストで混入を検出すること。
2. **フォントを必ず明示指定する。** 既定のシステムフォントはアルメニア文字を破綻して描画する。
   `Noto Sans Armenian` / `Noto Serif Armenian` を self-host し、フォールバックを設定する。
3. **文末は `։`（U+0589, վերջակետ）。** ラテンのピリオドやコロン（`:`）で代用しない。
4. **疑問符 `՞` と感嘆符 `՜` は文末ではなく、問われている語の最後の母音の上に置く。**
   これは学習者が最初につまずく点なので、文字モジュールで明示的に教えること。
5. **語頭の `ե` は /jɛ/、語頭の `ո` は /vo/ と読む。** 発音データは「綴り」と「実際の音」を
   別フィールドで持ち、規則で導出しない（例外があるため）。
6. **正書法上の合字と二重字：** `ու`（u）と `և`（ev）の扱いを最初に決め、全データで統一する。
   文字数を「38」と数えるか「39」と数えるかは数え方の問題であり、
   アプリ内で一貫していることが重要（`docs/curriculum.md` 参照）。
7. **ローマ字転写は補助輪。** 学習の主軸は常にアルメニア文字。転写を非表示にできる設定を必ず用意する。
8. **破裂音は三系列（有声／無気無声／帯気無声）。** `բ/պ/փ` は「バ／パ／パ」ではない。
   日本語の 2 項対立に潰して説明しない。詳細は `docs/phonetics.md`。
9. **アルメニア語とロシア語を同じ画面に併記しない。** 想起干渉が起きる。
   切り替え式にし、レビューキューも分ける。例外は借用語タグのみ（`docs/russian.md`）。
10. **アルメニア語に文法性はない。** ロシア語の性をアルメニア語に持ち込ませない。

---

## 7. コンテンツの正確性に関する絶対規則

**言語データを推測で埋めてはいけない。**

- 語彙・例文・活用形を生成するとき、確信が持てないものは `"status": "unverified"` を付け、
  UI 上で出題しない（レビュー待ちキューに入れる）。
- 各エントリに `source` フィールドを必須とする（辞書名・参照元）。
- 「それらしいアルメニア語」を大量生成するより、**検証済みの 50 語のほうが価値がある。**
- 曖昧な点はユーザーに質問する。黙って埋めない。

---

## 8. コーディング規約

- `any` 禁止。型が決まらない場合は `unknown` + 絞り込み。
- ドメインロジック（`src/domain/`）は React に依存しない純粋関数として書き、必ずテストを付ける。
  特に **格変化・動詞活用エンジンはテストファースト**で書くこと。
- コンポーネントは 150 行を超えたら分割を検討。
- コメントは「なぜ」を書く。「何を」はコードで表現する。
- 言語学的に非自明な処理には必ず出典コメントを付ける。

### 移植性（将来のネイティブ化に備えた保険。今のコストはほぼゼロ）
- `src/domain/` は React だけでなく **DOM・Web API にも依存しない。**
  `AudioContext` や `IndexedDB` を domain 内で呼ばない。
  音声解析は `Float32Array` を受け取る純粋関数として書く
- **永続化と音声取得はインターフェース越しに使う。**
  `StorageAdapter` / `AudioCaptureAdapter` を定義し、Dexie と Web Audio はその実装に留める
- `content/` に Web 固有の前提（URL、DOM、パス依存）を持ち込まない

---

## 9. 開発コマンド

```bash
npm run dev        # 開発サーバー
npm run build      # 本番ビルド
npm run test       # Vitest
npm run lint       # ESLint + tsc --noEmit
npm run validate   # content/ の Zod 検証（CI 必須）
```

**コミット前に `npm run lint && npm run test && npm run validate` が通ること。**

## 10. 作業の進め方

1. `docs/roadmap.md` のフェーズを **順番に** 進める。先のフェーズに手を出さない。
2. 各フェーズ開始時に、実装計画を箇条書きでユーザーに提示して合意を取る。
3. 1コミット＝1つの意味のある変更。フェーズ完了時に完了条件との対応を報告する。
4. 判断に迷ったら実装せずに質問する。特に次の 4 点：
   - **言語データの正確性**（推測で埋めない）
   - **参照した資料が東アルメニア語か**（西の資料が混ざっていないか）
   - **発音判定の閾値**（較正前の数値をハードコードしない）
   - **コストが発生しうる選択**（有料 API、無料枠を超える構成）
